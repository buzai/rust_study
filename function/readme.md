rust function use fn keywork

return is not needed, just use a expression without semicolo, it will auto return the expression result

```
fn [fn_name](paramsList) -> [resType] {
    <!-- logic -->
    <!-- expression res is resType -->
}
```
